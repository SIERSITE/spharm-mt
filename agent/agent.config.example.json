{
  "_doc": "Template de configuração do SPharm.MT agent. Copia para agent.config.json e edita. NUNCA commitar agent.config.json. Mais detalhes em INSTALL_WINDOWS.md.",
  "saas": {
    "endpoint": "https://app.spharmmt.app",
    "tenantSlug": "demo-neon",
    "ingestKey": "COLAR_KEY_64_HEX_DO_DEV",
    "farmacia": "Farmácia Central",
    "_healthcheckUrl_doc": "OPCIONAL — dead-man switch externo. URL base Healthchecks.io/cronitor. Agent posta /start, /fail e endpoint default. Recomendado em produção para detectar PC offline.",
    "healthcheckUrl": ""
  },
  "sqlServer": {
    "host": "localhost",
    "port": 1433,
    "database": "SPHARM",
    "user": "spharm_readonly",
    "password": "COLAR_PASSWORD_DO_LOGIN_READONLY",
    "encrypt": false,
    "trustServerCertificate": true
  },
  "options": {
    "outputDir": "output",
    "agentVersion": "0.1.0",
    "_ordersWriteMode_doc": "Como escrever encomendas finalizadas no SPharm: 'stub' (default — escreve JSON no outputDir/orders-export/, ack com docId STUB-...) ou 'insert' (INSERT real em dbo.Encomendas + dbo.[Encomendas Detalhe], requer secção ordersInsert abaixo e SQL login com db_datawriter).",
    "ordersWriteMode": "stub"
  },
  "_ordersInsert_doc": "OBRIGATÓRIO quando ordersWriteMode=insert. Operador SPharm preenche depois de validar com run-test-order-write.bat em dry-run. Valores típicos sugeridos abaixo — confirmar localmente em cada farmácia. IDEMPOTÊNCIA: gerida em tabela auxiliar dbo.SPharmMT_OrderWriteLog (criar com run-setup-orders-write-log.bat). NENHUMA coluna do SPharm é tocada para tracking.",
  "ordersInsert": {
    "_userIdForInsert_doc": "User ID SPharm (smallint) que aparece como 'criador' da encomenda. Usar um utilizador dedicado (criar via SPharm UI) ou um operacional existente.",
    "userIdForInsert": 0,
    "_fornecedorIdForOrders_doc": "Fornecedor ID SPharm default para encomendas vindas do SaaS. v1 limitation: 1 fornecedor por farmácia. Validar via SPharm UI a lista de fornecedores e escolher o pretendido.",
    "fornecedorIdForOrders": 0,
    "_armazemId_doc": "ArmazemID (tinyint). Default observado em todas as amostras = 1.",
    "armazemId": 1,
    "_tipoEncomendaId_doc": "TipoEncomendaID (tinyint). Default observado em todas as amostras = 2. Verificar em dbo.TipoEncomenda quais os valores válidos no SPharm local.",
    "tipoEncomendaId": 2,
    "_encomendaSituacaoInitial_doc": "EncomendaSituacaoID (char(1)) estado inicial. 'A' = Aberta (default). Operador pode usar outro valor — confirmar em dbo.[Enc_Situacao] ou equivalente.",
    "encomendaSituacaoInitial": "A",
    "_productLookupColumn_doc": "OBRIGATÓRIO. Nome da coluna em dbo.Stocks que contém o CNP INDIVIDUAL do produto. NUNCA 'CodCNPEM' (é grupo homogéneo, identifica conjuntos de produtos equivalentes — não produto individual). Correr 'run-inspect-product-identifiers.bat' para identificar a coluna real (provavelmente 'Codigo' ou 'Codigo Externo'). Valor vazio aqui bloqueia ordersWriteMode=insert com mensagem clara.",
    "productLookupColumn": ""
  }
}
